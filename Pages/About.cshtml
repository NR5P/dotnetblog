@page
@model dotnethomepage.Pages.AboutModel
@{
    ViewData["Title"] = "About me";
}

@foreach(Models.AboutItem item in Model.aboutList) {
    <div className="about-section">
        <h2>@item.title</h2>
        <p>
            @item.body
        </p>
    </div>
}
